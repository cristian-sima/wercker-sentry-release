# wercker.yml 

box: debian:stable-slim

build:
  steps:
    - script:
        name: prepare output
        code: rm -rf $WERCKER_ROOT/.git

publish:
  steps:
    - internal/publish-step:
        owner: wercker
